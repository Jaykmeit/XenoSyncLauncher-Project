<Application x:Class="XenoSyncLauncher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Paleta de color base del launcher (tema oscuro tipo "gaming") || #5B8CFF || -->
            <SolidColorBrush x:Key="BrushBackground" Color="#1B1D23"/>
            <SolidColorBrush x:Key="BrushPanel" Color="#24262E"/>
            <SolidColorBrush x:Key="BrushAccent" Color="#F7673E"/>
            <SolidColorBrush x:Key="BrushAccentHover" Color="#FF8866"/>
            <SolidColorBrush x:Key="BrushTextPrimary" Color="#F1F2F6"/>
            <SolidColorBrush x:Key="BrushTextSecondary" Color="#A0A4B0"/>
            <SolidColorBrush x:Key="BrushWarning" Color="#FFB454"/>
            <SolidColorBrush x:Key="BrushDanger" Color="#FF6B6B"/>
            <SolidColorBrush x:Key="BrushBorder" Color="#33353E"/>
            <SolidColorBrush x:Key="BrushAccentRun" Color="#66B0FF"/>
            <SolidColorBrush x:Key="BrushAccentRunHover" Color="#8CC6FF"/>
            <SolidColorBrush x:Key="BrushLive" Color="#90FF8C"/>

            <Style TargetType="Button" x:Key="WizardButton">
                <Setter Property="Background" Value="{StaticResource BrushPanel}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="Button" x:Key="PrimaryButton" BasedOn="{StaticResource WizardButton}">
                <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource BrushAccentHover}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style TargetType="Button" x:Key="RunButton" BasedOn="{StaticResource WizardButton}">
                <Setter Property="Background" Value="{StaticResource BrushAccentRun}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource BrushAccentRunHover}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
